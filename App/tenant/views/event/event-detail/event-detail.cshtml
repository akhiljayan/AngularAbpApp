<style type="text/css">
    #footer {
        position: fixed;
        bottom: 0;
        left: 0px;
        width: 100%;
        background-color: white;
        padding: 12px;
        border-top: 0.3px solid;
    }

    button.linkButton {
        padding: 0px;
    }

        button.linkButton:hover > i.custom-icon {
            color: white !important;
        }

    .button.linkButton.hover-underline:hover {
        text-decoration: underline;
    }
</style>


<div class="row margin-bottom-5">
    <div class="col-xs-12">
        <div class="page-head">
            <div class="page-title">
                <h1>
                    <span>@L("EventBookingAttendance")</span>
                </h1>
            </div>
        </div>
    </div>
</div>

@{
    var attributes = new Dictionary<string, object>();
    attributes.Add("role", "form");
    attributes.Add("id", "eventRegistrationForm");
    attributes.Add("name", "vm.form");
    attributes.Add("novalidate", "");
    attributes.Add("action", "#");
    attributes.Add("angular-validator-submit", "vm.saveEventRegistration()");
    attributes.Add("angular-validator", "");
}
@using (var f = Html.BeginForm(htmlAttributes: new HtmlAttributes(attributes).AddClass("form-validation horizontal-form")))
{
    using (var s = f.BeginSection())
    {
        <div class="row" style="margin-bottom: 80px;">
            <div class="col-md-12 col-sm-12">
                @Html.Partial("~/App/tenant/views/event/event-detail/components/general-card.cshtml")
            </div>
            <div class="col-md-12 col-sm-12" ng-class="{ 'div-disabled': vm.isNewRecord }" ng-if="!vm.isNewRecord">
                <person-registration mode="vm.mode"
                                     model="vm.model"
                                     show-delete="vm.updateDeleteFlag($event)"
                                     permissions="vm.permissions"></person-registration>
            </div>
        </div>
        @*<div class="row">
            <div class="col-md-12 col-sm-12" id="footer">
                <div class="form-actions margin-top-10 noborder pull-right">
                    <button type="submit" class="btn btn-circle light-grey-pcc btn-outline btn-small" ng-if="vm.mode=='edit'"><i class="material-icons ps-btn-material">check</i>Save</button>
                    <button type="button" class="btn btn-circle light-grey-pcc btn-outline btn-small" ng-if="vm.mode=='' && !vm.isNewRecord" ng-click="vm.mode='edit';" ng-disabled="vm.model.eventStatus == vm.eventStatus.Completed"><i class="material-icons ps-btn-material">edit</i>Edit</button>
                    <button type="button" class="btn btn-circle light-grey-pcc btn-outline btn-small" ng-if="vm.mode=='' && !vm.isNewRecord" ng-click="vm.close()"><i class="material-icons ps-btn-material">clear</i>Close</button>
                    <button type="button" class="btn btn-circle light-grey-pcc btn-outline btn-small" ng-if="vm.mode=='edit' && !vm.isNewRecord" ng-click="vm.mode = ''"><i class="material-icons ps-btn-material">clear</i>Close</button>
                    <button type="button" class="btn btn-circle light-grey-pcc btn-outline btn-small" ng-if="vm.mode=='edit' && vm.isNewRecord" ng-click="vm.close()"><i class="material-icons ps-btn-material">clear</i>Close</button>
                </div>
            </div>
        </div>*@
    }
}
