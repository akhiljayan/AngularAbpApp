<membership-detail mode="vm.mode"
                   model="vm.model"
                   loading="vm.loading"
                   on-update="vm.updateMembership($event)"
                   on-copy="vm.copyMembership($event)"
                   on-terminate="vm.terminateMembership($event)"
                   on-delete="vm.deleteMembership(event)"
                   on-reload="vm.load()">
</membership-detail>

