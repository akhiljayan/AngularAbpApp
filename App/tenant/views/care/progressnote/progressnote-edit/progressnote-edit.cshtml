<progressnote-detail mode="$ctrl.mode"
                     model="$ctrl.model"
                     resolve="$ctrl.resolve"
                     saving="$ctrl.saving"
                     on-save="$ctrl.updateProgressNote(event)"
                     on-cancel="$ctrl.cancel()">
</progressnote-detail>