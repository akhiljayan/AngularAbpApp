<progressnote-detail mode="$ctrl.mode"
                     model="$ctrl.model"
                     resolve="$ctrl.resolve"
                     selectedPersonProblems="$ctrl.selectedPersonProblems"
                     saving="$ctrl.saving"
                     on-save="$ctrl.createProgressNote(event)"
                     on-cancel="$ctrl.cancel()">
</progressnote-detail>